import { NextPage } from "next"
import { useSession } from "next-auth/react";
import { trpc } from "../utils/trpc";


const Reg:NextPage = () => {


    return(
        <p>elo2w</p>
    )
}

export default Reg
